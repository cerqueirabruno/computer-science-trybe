# MÓDULOS: arquivo python, que contém funções e classes que podem
#   ser importadas para outros arquivos python;

# PACOTE: são módulos python que contêm outros módulos e/ou pacotes;

# AMBIENTE VIRTUAL(VENV);
# definição: é um ambiente isolado, onde podemos instalar pacotes e módulos;
# criação do ambiente (.venv é o nome do ambiente): $ python3 -m venv .venv
# instalação do venv: $ sudo apt-get install python3-venv
# ativação do ambiente: $ source .venv/bin/activate
# validar ambiente ativo: $ which python3
